name: Build

on:
  push:
    tags-ignore:
      - "**"
    branches:
      - master
  pull_request:
    types:
      - opened
      - synchronize

jobs:
  build:
    name: Build
    uses: azazeal/workflows/.github/workflows/golib.yml@v1.0.0
